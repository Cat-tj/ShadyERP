DATABASE_URL="postgresql://user:password@host:5432/database"
# Opsional: kalau diisi, dipakai KHUSUS untuk `prisma migrate deploy`
# (mis. connection string pooler Supabase di DATABASE_URL, direct di sini).
# Kosongkan/hapus untuk dev lokal biasa — DATABASE_URL sudah cukup.
DIRECT_URL="postgresql://user:password@host:5432/database"
AUTH_SECRET="ganti-dengan-secret-acak-yang-panjang"
