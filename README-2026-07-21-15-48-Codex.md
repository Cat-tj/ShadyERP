# Update 2026-07-21 15:48 - Codex

## Scope

Memindahkan navigasi submenu Pengaturan dari tab horizontal ke sidebar desktop,
dengan menu indeks yang sesuai untuk layar kecil.

## Changes

- Menambahkan satu sumber data `src/features/settings/settings-navigation.ts`.
- Menambahkan `SettingsSidebarNav` untuk sidebar desktop pada `AppShell`.
- Menghapus `SettingsTabs` dan tab horizontal dari layout Pengaturan.
- Mengubah indeks `/pengaturan` menjadi server-rendered agar modul yang
  dinonaktifkan tidak muncul di menu layar kecil.
- Menyesuaikan tombol kembali agar tersedia pada seluruh breakpoint tanpa
  sidebar desktop.

## Behavior

- Desktop: Pengaturan membuka submenu vertikal di sidebar kiri.
- Mobile dan tablet kecil: `/pengaturan` menjadi daftar menu; halaman detail
  mempunyai tombol kembali ke menu Pengaturan.
- Modul nonaktif disembunyikan dari sidebar dan daftar layar kecil melalui
  sumber policy yang sama.

## Verification

- `./node_modules/.bin/eslint` pada semua file yang diubah: pass.
- `./node_modules/.bin/tsc --noEmit`: pass.
- `git diff --check`: pass.

## Not Performed

- Tidak ada push, deploy, migrasi database, atau perubahan authorization.
- Browser screenshot belum dijalankan karena worktree agent terisolasi tidak
  membawa environment autentikasi lokal.
