# Update Codex - 14 Juli 2026 12:00 WITA

## POS/Kasir redesign

- Desktop memakai workspace dua panel: katalog produk dan cart sticky 380px.
- Tablet memakai tombol checkout sticky serta cart drawer dari kanan.
- Mobile memakai grid produk dua kolom, kartu ringkas, dan checkout bar di atas bottom navigation.
- Product card dibuat lebih padat: ukuran visual diperkecil, stok normal disembunyikan, serta quantity control hanya tampil ketika produk sudah dipilih.
- Search, shortcut `F2` dan `Escape`, scanner status, kategori horizontal, copy pelanggan, serta menu shift diperbarui.
- Tombol `Gesek tunai` dan `Tutup shift` dipindahkan ke menu Shift; `Riwayat` menjadi `Transaksi`.

## Verifikasi

- `npx eslint src/components/kasir/pos-screen.tsx` selesai tanpa temuan.
- `npm run build` selesai menggunakan environment build lokal tanpa secret production.
- Tampilan desktop, tablet, dan mobile diperiksa melalui environment audit terisolasi.

## Catatan

- Tidak ada deployment production, commit, atau push GitHub pada update ini.
