# 2026-07-14 13:02 - Codex

## Deploy VPS

- Production repository `/home/ubuntu/ShadyERP` berpindah ke branch `codex/erp-audit-baseline` pada commit `c5c6ef6`.
- Backup PostgreSQL dibuat sebelum perubahan skema di `/home/ubuntu/backups/shadyerp-pre-c5c6ef6-20260714-125352.dump`.
- Riwayat Prisma lama diselaraskan hanya setelah diff skema membuktikan struktur yang bersangkutan memang sudah ada di database.
- Enam migrasi hardening 13 Juli berhasil diterapkan: PIN hash, session version, sale idempotency key, journal source key, pembatasan satu shift terbuka, dan invoice sequence.
- Backfill PIN memproses 2 akun legacy.
- `npm run build`, restart PM2 `shadyerp`, health endpoint lokal, dan health endpoint publik berhasil.
- Landing publik diverifikasi memuat business showcase baru.

## Catatan

- PM2 masih menyimpan beberapa log `Failed to find Server Action` dari request browser versi deployment lama. Proses baru berstatus online dan tidak mengalami unstable restart. Bila pengguna melihat error form segera setelah deploy, reload penuh halaman akan memuat action ID baru.
