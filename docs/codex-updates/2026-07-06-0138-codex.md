# Update 2026-07-06 01:38 - Codex

## Ringkasan
- Memperbaiki generator QRIS dinamis agar QRIS statis asli yang memiliki spasi pada nama merchant tetap valid.
- Menambahkan normalisasi payload QRIS sebelum disimpan di Pengaturan Bisnis.
- Mengubah QRIS hasil generate dari mode statis `010211` ke mode dinamis `010212`.

## Detail Teknis
- `src/lib/qris-dynamic.ts`
  - Normalisasi tidak lagi menghapus semua spasi karena spasi bisa menjadi bagian valid dari data merchant QRIS.
  - Menambahkan parser tag EMV top-level untuk mencari dan memvalidasi tag CRC.
  - Menambahkan `normalizeStaticQrisPayload()` agar hasil scan disimpan dalam bentuk payload bersih.
- `src/app/(app)/pengaturan/bisnis/actions.ts`
  - Submit pengaturan sekarang menyimpan QRIS yang sudah dinormalisasi.
  - Validasi QRIS tetap mencoba membuat QRIS dinamis nominal tes sebelum data disimpan.

## Verifikasi
- Payload QRIS contoh dari gambar user berhasil dinormalisasi.
- QRIS dinamis nominal Rp64.000 berhasil dibuat dan mengandung tag nominal `540564000`.
- Mode QRIS hasil generate berubah menjadi dynamic `010212`.
