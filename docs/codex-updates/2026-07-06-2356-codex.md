# 2026-07-06 23:56 - Codex Update

## Ringkasan

Merapikan prioritas tinggi setelah crosscheck update Antigravity: Simple Mode dibuat lebih ringan, offline queue dibuat terlihat oleh user, dan koreksi metode bayar POS diberi audit.

## Implementasi

- `src/components/simple-shell.tsx`
  - Bottom navigation Simple Mode dikembalikan ke 4 tab: Kasir, Uang, Data, Lainnya.
  - Akses `/simple/hari-ini` tetap lewat logo/topbar dan menu agar mobile tidak penuh.

- `src/app/(app)/layout.tsx`
  - Menghapus query `getSimpleAlerts()` dari layout global Simple Mode.
  - Ini menghindari halaman Kasir ikut menarik stok, expiry, hutang, shift, dan order setiap render.

- `src/app/pilih-aplikasi/page.tsx`
  - Tenant SIMPLE sekarang diarahkan ke `/simple/hari-ini`, bukan langsung `/kasir`.

- `src/components/simple/offline-queue-panel.tsx`
  - Panel client-side untuk membaca IndexedDB offline POS.
  - Menampilkan status online/offline, jumlah pending sync, total pending, retry manual, dan transaksi gagal.

- `src/app/(app)/simple/offline/page.tsx`
  - Halaman baru `/simple/offline` untuk memantau transaksi kasir yang belum terkirim.

- `src/app/(app)/simple/menu/page.tsx`
  - Menambahkan pintasan Hari Ini, Alert Center, dan Offline Sync.

- `src/server/services/sale-service.ts`
  - Menambahkan `correctSalePaymentMethod()` untuk koreksi metode bayar non-deposit dengan alasan wajib.

- `src/app/(app)/kasir/riwayat/actions.ts`
  - Menambahkan action koreksi metode bayar.

- `src/components/kasir/riwayat-list.tsx`
  - Menambahkan tombol **Koreksi bayar** di transaksi selesai untuk Owner/Manager.

- `prisma/schema.prisma`
  - Menambahkan audit action `SALE_PAYMENT_CORRECTION`.

- `prisma/migrations/20260706224500_add_sale_payment_correction_audit_action/migration.sql`
  - Migration enum audit action untuk koreksi pembayaran.

## Catatan

- Koreksi ke/dari `DEPOSIT` sengaja tidak dibuka karena memengaruhi saldo member. Gunakan retur/refund manual untuk kasus itu.
- Alert count tidak lagi dipasang di bottom tab agar kasir tetap ringan untuk device low-spec.
