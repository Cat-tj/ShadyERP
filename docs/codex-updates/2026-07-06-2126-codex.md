# 2026-07-06 21:26 - Codex Update

## Ringkasan

Pembersihan menyeluruh terhadap seluruh *linting debt* (utang kode) di codebase dan sinkronisasi berkas Prisma Client. Saat ini `npm run lint`, `npx tsc --noEmit`, dan `npx next build` semuanya sukses tanpa error.

## Implementasi

### 1. Sinkronisasi Tipe Prisma Client
* Menjalankan `npx prisma generate` untuk mensinkronisasi tipe data local `@prisma/client` dengan skema terbaru (menghilangkan error hilangnya properti `superAdminAuditLog` di TypeScript compilation).

### 2. Perbaikan Berkas CRM
* **`src/app/(app)/crm/actions.ts`**: Menghapus tipe impor `LeadInput` yang tidak digunakan dan mengganti penanganan error `catch (error: any)` dengan pengecekan type-safe `error instanceof Error`.

### 3. Perbaikan Berkas Finance & Hutang Supplier
* **`src/app/(app)/finance/hutang-supplier/actions.ts`**: Menghapus deklarasi variabel `today` yang tidak digunakan dan mendesain ulang penanganan error block `catch` agar type-safe.
* **`src/app/(app)/finance/hutang-supplier/page.tsx`**: Mengganti casting parameter bertipe `any` di mapper `.map((inv: any) => ...)` menjadi type-safe `(typeof rawInvoices)[number]`.
* **`src/app/(app)/finance/kas/actions.ts`**: Menghapus impor `CashFlowInput` yang tidak digunakan dan membersihkan penanganan error `catch`.

### 4. Perbaikan Komponen CRM
* **`src/components/crm/lead-manager.tsx`**: Mengganti type-casting `as any` pada selector status pipeline menjadi tipe literal yang aman (`as "NEW" | "CONTACTED" | "WON" | "LOST"`).

### 5. Perbaikan Akuntansi & Kasir
* **`src/server/services/accounting-service.ts`**:
  * Mengimpor `Prisma` dan mengubah parameter transaksi database `tx?: any` menjadi `tx?: Prisma.TransactionClient`.
  * Memperbaiki error properti `expense.title` yang tidak terdefinisi di skema menjadi `expense.note || expense.category`.
  * Menghapus deklarasi variabel `subtotal` yang tidak digunakan di fungsi `logSaleToJournal`.
* **`src/server/services/sale-service.ts`**: Mengubah deklarasi variabel dari `let po: any` ke inferensi otomatis, serta mengubah type-casting `i: any` menjadi `i: { productId: string }`.

## Verifikasi

* **`npm run lint`**: Sukses (Zero errors & warnings).
* **`npx tsc --noEmit`**: Sukses (Zero compile errors).
* **`npx next build`**: Sukses merender 75 halaman/routes statik dan dinamis.
