# 2026-07-06 20:37 - Codex Update

## Ringkasan

Melanjutkan hardening Simple Mode dan Superadmin.

## Implementasi

### Superadmin

- Menambahkan service akun superadmin:
  - `listSuperAdmins()`
  - `createSuperAdminAccount()`
  - `changeSuperAdminPassword()`
- Menambahkan Server Action:
  - `createSuperAdminAccountAction`
  - `changeSuperAdminPasswordAction`
- Menambahkan halaman `/superadmin/admins`.
  - Tambah akun superadmin baru.
  - Reset password superadmin yang sudah ada.
  - Jika email sudah ada, action akan update nama dan password.
- Menambahkan link `Tenant` dan `Admin` di header superadmin.

### Simple Mode

- `/simple/data` ditambah:
  - Produk sepi / worst sales 30 hari.
  - Insight restock dari stok menipis.
  - Insight batch expired dalam 14 hari.
- Bottom tab `Data` diberi badge alert kecil agar owner mudah melihat insight.

## Catatan

- Tidak ada migrasi database baru.
- Audit superadmin yang proper masih disarankan memakai tabel terpisah nanti.
- Permission granular penuh belum dikerjakan karena perlu desain role matrix yang lebih hati-hati.

## Verifikasi

- `npx tsc --noEmit` berhasil.
- `npm run build` berhasil.
- `npm run lint` masih gagal karena lint debt lama yang sama seperti update sebelumnya:
  - `src/app/(app)/crm/actions.ts`
  - `src/app/(app)/finance/hutang-supplier/actions.ts`
  - `src/app/(app)/finance/hutang-supplier/page.tsx`
  - `src/app/(app)/finance/kas/actions.ts`
  - `src/components/crm/lead-manager.tsx`
  - `src/server/services/accounting-service.ts`
  - `src/server/services/sale-service.ts`

## Next

- Tambah tabel audit superadmin.
- Buat role matrix per tenant: kasir, finance, HR, manager cabang.
- Buat alert badge Data yang benar-benar data-driven, bukan statis.
