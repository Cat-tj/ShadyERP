# 2026-07-06 19:13 - Codex Update

## Ringkasan

Mengimplementasikan bagian utama dari handover pending:

- Altora Simple Mode UX.
- Enhanced Superadmin Dashboard.

## Implementasi

### Simple Mode UX

- Menambahkan `src/components/simple-shell.tsx`.
  - Topbar tipis dengan nama tenant, akun, dan logout.
  - Bottom tab bar 4 menu:
    - Kasir -> `/kasir`
    - Uang -> `/simple/uang`
    - Data -> `/simple/data`
    - Lainnya -> `/simple/menu`
- Mengubah `src/app/(app)/layout.tsx`.
  - `accountingMode === "SIMPLE"` memakai `SimpleShell`.
  - `accountingMode === "ADVANCED"` tetap memakai `AppShell`.
- Mengubah `src/app/pilih-aplikasi/page.tsx`.
  - Tenant Simple langsung redirect ke `/kasir`.
  - Tenant Advanced tetap melihat hub picker.

### Halaman Simple

- Menambahkan `/simple/uang`.
  - Ringkasan tunai POS hari ini.
  - Uang keluar shift.
  - Estimasi uang laci.
  - Jumlah shift terbuka.
  - Form cepat catat uang masuk/keluar.
  - Riwayat cashflow 7 hari.
- Menambahkan `/simple/data`.
  - Omzet hari ini vs kemarin.
  - Selisih omzet.
  - 5 produk terlaris dengan bar sederhana.
  - Alert stok menipis dari reorder point outlet pertama.
- Menambahkan `/simple/menu`.
  - Grid fitur sekunder sesuai role dan modul aktif:
    - Produk
    - Inventory
    - Member
    - Absensi
    - Laundry
    - Finance
    - Pengaturan

### Superadmin

- Menambahkan server actions:
  - `changeTenantPlanAction`
  - `changeTenantAccountingModeAction`
- Mengubah halaman `/superadmin`.
  - Menampilkan widget statistik platform dari `getPlatformStats()`.
  - Tenant row sekarang mendapat `accountingMode`.
- Mengubah `TenantListManager`.
  - Dropdown paket Free/Basic/Pro per tenant.
  - Toggle mode Simple/Advanced per tenant.
  - Link Detail per tenant.
- Menambahkan halaman `/superadmin/tenant/[id]`.
  - Statistik tenant: outlet, user, produk, omzet 30 hari.
  - Status aktif/suspend.
  - Modul nonaktif.
  - Riwayat request langganan.

## Catatan Teknis

- Tidak menambah dependency baru.
- Simple mode memakai service yang sudah ada.
- Belum dibuat visual chart kompleks; bar sederhana cukup untuk low-spec friendly.
- `/simple/data` menghitung omzet hari ini dan kemarin dengan range Jakarta.

## Verifikasi

- `npx tsc --noEmit` berhasil.
- `npm run build` berhasil.
- Smoke check `curl -I` ke `/simple/uang`, `/simple/data`, dan `/superadmin` berhasil merespons. Redirect ke login muncul karena request tidak membawa sesi, sesuai guard auth.
- `npm run lint` masih gagal karena lint debt yang sudah ada di luar perubahan ini:
  - `src/app/(app)/crm/actions.ts`
  - `src/app/(app)/finance/hutang-supplier/actions.ts`
  - `src/app/(app)/finance/hutang-supplier/page.tsx`
  - `src/app/(app)/finance/kas/actions.ts`
  - `src/components/crm/lead-manager.tsx`
  - `src/server/services/accounting-service.ts`
  - `src/server/services/sale-service.ts`
  - File baru di update ini tidak lagi muncul di error lint setelah `simple/uang/actions.ts` dibersihkan dari `any`.

## Next

- Bersihkan lint debt lama jika ingin CI lint hijau.
- Tes browser:
  - Tenant SIMPLE: `/pilih-aplikasi` harus langsung masuk `/kasir`.
  - Tenant SIMPLE: bottom tab harus muncul.
  - Tenant ADVANCED: hub picker dan sidebar harus tetap normal.
  - Superadmin: dropdown paket, toggle mode, dan detail tenant.
