# Update Audit Loop - 13 Juli 2026 22:00 WITA

## Tujuan
Mengubah audit Altora dari daftar temuan pasif menjadi loop implementasi yang dapat dilanjutkan antar-agent tanpa melewati risiko P0/P1.

## Perubahan
- Menambahkan `docs/audit/WORK-QUEUE.md` dengan wave, acceptance criteria, test gate, dan aturan penutupan ticket.
- Menambahkan bagian **Audit Hardening Loop** ke `Roadmap.md`.
- Mengoreksi klaim Roadmap tentang lint: baseline aktual memiliki 18 error, sehingga status kembali terbuka.
- Mengunci perbaikan P0 supplier tenant-scoped sebagai ticket selesai secara lokal.

## Validasi
- `npm run build` lulus menggunakan `DATABASE_URL` dummy untuk generate Prisma saja.
- `npm run lint` gagal dengan 18 error yang terdokumentasi; tidak dianggap selesai.

## Next Ticket
`W0.2`: bersihkan 18 lint error secara teruji, lalu tambah database integration tests untuk tenant isolation dan checkout bersamaan.

## Follow-up: PIN hardening migration
- Added a two-stage User PIN migration: the old `pin` column is read only as `legacyPin`, while new PINs are stored solely in bcrypt `pinHash`.
- Employee list responses no longer select password hashes or either PIN field.
- Employee forms no longer prefill a stored PIN; a PIN is only sent when a new six-digit value is entered.
- Added `db:backfill-legacy-pins`; run it only after `prisma migrate deploy` and a database backup.
- The legacy column remains temporarily for safe backfill. Removing it is a later, production-verified migration.

## Follow-up: session and stock integrity
- Added `User.sessionVersion`, embedded in the login JWT and verified server-side on every protected app request. Credential, role/outlet, PIN, and active-status changes invalidate old sessions.
- Replaced POS stock decrements with tenant-scoped conditional updates (`qty >= needed`) inside the sale transaction. Concurrent checkout now rolls back instead of allowing negative stock.
- Unit tests and the production Next build pass locally after these changes. The production database migrations and browser smoke tests remain required before deployment.

## Final validation snapshot
- `npm run lint`: pass with 0 errors (23 non-blocking warnings).
- `npm run test:unit`: pass (9 tests across supplier isolation and PIN validation).
- `npm run build`: pass.
- `npm run verify:release`: lint and unit tests passed; build was re-run separately after a typed backup DTO correction and passed.
- No migration, deployment, or push was performed.

## Follow-up: POS retry protection
- Added nullable tenant-scoped `Sale.idempotencyKey` and migration `20260713223000_add_sale_idempotency_key`.
- `createSale` returns an existing tenant sale for the same key before creating stock, payment, loyalty, or accounting effects.
- Offline queue now creates and retains the same idempotency key during repeated sync attempts.
- A duplicate-key race is also recovered after transaction rollback by loading the winning sale, so simultaneous retries converge to one transaction.
- Prisma generation, 9 unit tests, and production build pass locally. A concurrent database integration test and production migration verification remain required.

## Follow-up: accounting posting idempotency
- Added nullable tenant-scoped `JournalEntry.sourceKey` and migration `20260713224000_add_journal_source_key`.
- Automatic revenue, discount, COGS, and expense postings now use stable source-line keys and return an existing entry on retry.
- Unit tests and production build pass locally. Journal balancing/reversals and real database integration tests remain pending.

## Follow-up: document ACL
- Fixed same-tenant document disclosure: the detail route now requires uploader, signer, explicit user access, or role access.
- Replaced raw auth reads in document actions with `requireSession`, so disabled/revoked accounts cannot bypass the server-side session check.
- Production build passes locally. Dedicated ACL tenant/user matrix tests and object-storage migration remain pending.

## Follow-up: ACL regression tests
- Added document access regression tests that assert both tenant scope and uploader/signer/explicit user-or-role predicates.
- `npm run test:unit` passes with 11 tests; `npm run lint` remains 0 errors.

## Follow-up: production operation
- Added `docs/audit/PRODUCTION-ROLLOUT.md`: preflight, migration order, PIN backfill, smoke checks, verification SQL, rollback, and explicit non-goals.
- Updated the release checklist so migrations cannot be treated as an ordinary frontend deploy.

## Follow-up: shift concurrency
- Added migration `20260713225000_enforce_one_open_shift` with a PostgreSQL partial unique index for one OPEN shift per tenant/user.
- Shift opening now validates the outlet tenant and translates unique conflicts into the proper cashier message.
- Shift closing now conditionally transitions OPEN to CLOSED once, preventing concurrent close requests from overwriting reconciliation data.

## Follow-up: atomic POS invoice sequence
- Added `InvoiceSequence` and migration `20260713226000_add_invoice_sequence`.
- POS now allocates invoice sequence with a tenant/outlet/day atomic upsert rather than counting already-created sales, removing the concurrent invoice-number collision path.
- Prisma generation and unit tests pass locally; database concurrency coverage remains pending.

## Follow-up: POS price authority
- Replaced public cashier item input with `PosCartItemInput`, which excludes internal `unitPriceOverride` and `variantLabel` snapshot fields.
- POS browser requests now rely on server-side price/variant/promo/tax calculation; internal table and catering services retain their separate server-only `createSale` pathway.

## Follow-up: mutation session guards
- Replaced raw JWT reads in supplier and purchase-order server actions with revocation-aware `requireSession`.
- Suspended tenants, inactive users, and stale session versions now cannot continue these procurement mutations merely because their JWT has not expired.
- Production build passes locally.

## Follow-up: inventory mutation session guards
- Replaced raw JWT reads in stock-receipt and stock-count actions with `requireSession`.
- Receipt creation/QC/completion/rejection and stock-count creation/update/completion/verification now honor tenant suspension, user deactivation, and session-version revocation.
- Unit tests pass locally (11 tests).

## Release-gate evidence refresh
- Full `npm run verify:release` completed locally with `DATABASE_URL` pointing at a non-production placeholder: lint has 0 errors, 11 unit tests pass, and the production Next build completes.
- A source sweep found no raw `auth()` calls in protected application actions; remaining uses are only the NextAuth route and login/register actions.
- This is not production-migration evidence. The rollout checklist, backup, migration apply, and smoke checks are still mandatory before promotion.

## Integration environment constraint
- Local runtime has no PostgreSQL/Docker test service. No Supabase/VPS production database was used as a substitute.
- Added explicit isolated database, shadow-database, reset, and required concurrency/tenant test cases to `docs/audit/TEST-MATRIX.md`.
- Prisma migration-directory diff correctly refuses a dummy URL without a configured shadow database; this is documented as an unmet test-environment requirement, not treated as a passing check.

## Follow-up: runtime configuration health
- Added a non-secret runtime configuration checker for `DATABASE_URL` and `AUTH_SECRET`.
- `/api/health` returns 503 with missing key names only when configuration is incomplete, separating deployment misconfiguration from database availability failures.
- Unit test suite passes with 13 tests; production build passes with a non-production test secret.

## Handover status
- Added `docs/audit/IMPLEMENTATION-STATUS.md` as a source-of-truth list of completed local controls, validation evidence, required promotion gates, and explicit remaining risk.

## Follow-up: isolated PostgreSQL integration gate
- Installed PostgreSQL 16 on the VPS exclusively for audit use. It is bound to `127.0.0.1` and uses a separate `altora_audit` role, database, and shadow database; production Supabase, production repository, PM2 processes, and Caddy were not modified.
- Synced the audit checkout to `/home/ubuntu/ShadyERP-audit`; all 64 Prisma migrations applied successfully from an empty database.
- Added `src/server/services/erp-integrity.integration.test.ts` and `npm run test:integration`. Four real database tests pass: tenant A/B supplier and sale isolation, concurrent same-key checkout idempotency, non-negative stock race, and one-open-shift race.
- `verify:release` now requires lint, unit tests, database integration tests, and build. It must be run with isolated database environment variables and still needs an E2E smoke stage before production promotion.

## Follow-up: online POS retry integrity
- `PaymentSheet` now allocates one browser idempotency key per payment attempt and reuses it for retries or offline queueing, closing the duplicate-checkout path outside the prior offline-only flow.

## Validation: isolated release gate
- On the isolated VPS audit checkout, `npm run lint` completed with 0 errors (23 existing warnings), `npm run test:unit` passed 15 tests, and `npm run test:integration` passed all 4 PostgreSQL integrity tests.
- `npm run verify:release` then completed its lint, unit, integration, and production build stages using only the audit database environment. A fresh `.next/BUILD_ID` exists in `/home/ubuntu/ShadyERP-audit`; no production process was restarted.
- `git diff --check` must remain clean before any commit. Production migration and deploy remain intentionally excluded until the operator takes and verifies a production backup.

## Production migration history repair
- Production migration status found one historical row (`20260706102200_add_laundry_orders`) absent from the current branch. Its database checksum exactly matches the historical file from commit `c0b0cee`.
- Restored that exact migration file to the audit checkout. This repairs source history only; it does not run SQL or mutate Supabase. Production backup `altora-pre-hardening-20260714-102114.dump` was created and verified with PostgreSQL 17 tooling before any promotion work.

## Production-like migration rehearsal
- Production schema had the 2026-07-10 through 2026-07-12 features but lacked their Prisma history rows. The full production backup was restored into an isolated PostgreSQL 17 clone on VPS localhost; the clone is not publicly reachable.
- Marking those already-present migrations as applied after a zero-diff schema check allowed the six new hardening migrations to apply normally. The clone ended at 65 migrations, `Database schema is up to date`, and an empty database-to-schema diff.
- The integrity suite passed 4/4 on the production-like clone. PIN backfill ran on the clone only, processed 2 accounts, left 0 legacy PIN values, and produced 2 hashes out of 9 users.
- Production has not been changed. The required production order is history reconciliation and new migrations, matching code deployment, then PIN backfill and manual smoke checks.

## Follow-up: billing webhook integrity
- Webhook verification now rejects stale/non-numeric timestamps and uses timing-safe HMAC comparison.
- Successful payment processing requires the invoice's provider payment link, amount, and currency to match the signed payload; paid invoices short-circuit safely.
- Payment history uses provider payment-link upsert inside the subscription update transaction to make duplicate webhooks converge.
- Final validation after the payment-history upsert: 13 unit tests and production build pass locally.
- Sumopod webhook route now validates signed JSON payload shape, supported status, non-negative integer amount, and IDR currency before invoking billing service; unit tests and build remain green.
- Added regression coverage for accepted fresh HMAC payloads and rejected malformed/stale timestamps; unit suite now passes with 15 tests.

## Follow-up: document signer integrity
- Signing initiation now requires at least one unique, active signer from the same tenant and rejects existing signer duplicates.
- Document status transition and signer creation run in one transaction, preventing a partially initialized signature workflow.
- Unit tests and production build pass locally.

## Follow-up: journal value invariant
- `postJournalEntry` now rejects non-integer/zero/negative amounts and equal or empty debit/credit account codes before writing.
- Unit tests and production build pass locally.
