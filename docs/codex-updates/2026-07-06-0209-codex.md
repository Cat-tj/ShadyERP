# Update 2026-07-06 02:09 - Codex

## Ringkasan
- Menambahkan metadata produk untuk kebutuhan retail, barbershop, toko listrik, dan F&B.
- Produk sekarang bisa dibedakan menjadi barang fisik dan jasa.
- Produk barang bisa punya SKU/barcode, tracking expired, masa simpan, dan garansi.
- Produk jasa bisa punya durasi layanan, cocok untuk barbershop/salon.
- Inventory menampilkan alert batch yang mendekati expired.

## Detail Teknis
- `prisma/schema.prisma`
  - Menambahkan enum `ProductKind`.
  - Menambahkan field `kind`, `trackExpiry`, `shelfLifeDays`, `warrantyDays`, dan `serviceDurationMin` di `Product`.
- `prisma/migrations/20260706101200_add_vertical_product_fields/migration.sql`
  - Migration untuk field produk lintas jenis usaha.
- `src/components/produk/product-form-modal.tsx`
  - Menambahkan input SKU/barcode.
  - Menambahkan pilihan `Barang` dan `Jasa`.
  - Menambahkan field expired, masa simpan, garansi, dan durasi layanan.
  - Produk jasa otomatis tidak memakai stok.
- `src/components/produk/produk-manager.tsx`
  - Menampilkan badge layanan, expired, garansi, dan masa simpan pada daftar produk.
- `src/app/(app)/inventory/page.tsx`
  - Menampilkan batch yang akan expired dalam 14 hari.
- `src/server/services/product-service.ts`
  - Menyimpan metadata produk baru.
- `src/app/(app)/produk/actions.ts`
  - Menambahkan validasi produk barang/jasa.
  - Create produk sekarang mengembalikan `id` agar stok awal bisa langsung disimpan.

## Verifikasi
- `npx prisma generate` lolos.
- `npx prisma migrate deploy` berhasil apply migration ke database Supabase lokal.
- `npx eslint` lolos.
- `npx tsc --noEmit` lolos.
- `npx next build` lolos.
