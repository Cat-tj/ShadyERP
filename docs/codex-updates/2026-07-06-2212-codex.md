# 2026-07-06 22:12 - Codex Update

## Ringkasan

Mengimplementasikan fitur **Export Backup Tenant** untuk memberikan keamanan cadangan data bagi pemilik usaha.

## Implementasi

### 1. Server Action Export
* **`src/app/(app)/pengaturan/bisnis/actions.ts`**:
  * Menambahkan server action `exportTenantBackupAction` dengan otorisasi ketat (`requireRole(["OWNER"])`).
  * Mengekstrak seluruh relasi data tenant dari database secara asinkron (`Promise.all`): Profil Bisnis, Konfigurasi, Outlet, Kategori, Produk, Transaksi Penjualan & Retur, Supplier, serta Member/Pelanggan.
  * Mengembalikan seluruh data tersebut dalam satu struktur JSON terformat lengkap dengan metadata timestamp dan versioning.

### 2. Antarmuka Bisnis Form (UI)
* **`src/components/pengaturan/bisnis-form.tsx`**:
  * Menambahkan kartu section baru **"Backup & Ekspor Data"** di bagian bawah pengaturan bisnis.
  * Mengatur *trigger click* untuk mengekspor data via Server Action, mengubah status tombol menjadi loading, dan memicu unduhan file JSON (`altora_backup_[slug]_[date].json`) secara dinamis di sisi client menggunakan Web Blobs.

## Verifikasi

* **`npx tsc --noEmit`**: Sukses (Zero compile errors).
* **`npx next build`**: Sukses merender 78 halaman/routes statik dan dinamis.
