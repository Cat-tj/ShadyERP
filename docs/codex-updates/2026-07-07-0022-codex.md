# 2026-07-07 00:22 - Codex Update

## Ringkasan

Menjalankan paket **Full Engine** operasional: receiving barang langsung untuk retail, batch/expired tracking dari penerimaan, dan ringkasan rekonsiliasi metode bayar di Buku Kas.

## Implementasi

- `prisma/schema.prisma`
  - Menambahkan `batchNumber` dan `expirationDate` di `StockReceiptItem`.

- `prisma/migrations/20260707001000_add_stock_receipt_item_batch_fields/migration.sql`
  - Migration untuk menyimpan batch/expired per item penerimaan barang.

- `src/server/services/stock-receipt-service.ts`
  - Menambahkan `createDirectStockReceipt()`:
    - User bisa mencatat barang datang tanpa membuat PO manual.
    - Sistem otomatis membuat PO cepat (`PO-DIRECT-*`) dan Stock Receipt.
    - Supplier opsional; jika kosong memakai/membuat `Supplier Umum`.
  - `completeReceipt()` sekarang membuat `StockBatch` jika item punya batch, expired date, atau produk mengaktifkan expiry tracking.

- `src/app/(app)/stock-receipt/actions.ts`
  - Menambahkan `createDirectStockReceiptAction()`.

- `src/app/(app)/stock-receipt/page.tsx`
  - Memuat produk stok dan supplier aktif untuk form penerimaan langsung.

- `src/components/stock-receipt/stock-receipt-manager.tsx`
  - Tombol **Terima barang** sekarang membuka form barang datang langsung.
  - Form mendukung outlet, supplier, produk, qty, modal/unit, batch/lot, expired date, dan catatan.

- `src/app/(app)/finance/kas/page.tsx`
  - Menambahkan panel **Rekonsiliasi metode bayar**:
    - Total nominal per CASH/QRIS/TRANSFER/EWALLET/DEPOSIT.
    - Jumlah transaksi.
    - Persentase kontribusi.

## Catatan

- Role granular belum dibongkar sekarang karena perlu migrasi enum dan update gate di banyak route/action. Lebih aman dijadikan refactor khusus berikutnya.
- Foto nota dan scan barcode belum dibuat; form saat ini memakai input produk/manual dulu agar workflow barang datang sudah bisa dipakai.
