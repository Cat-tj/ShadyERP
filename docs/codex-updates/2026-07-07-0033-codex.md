# 2026-07-07 00:33 - Codex Update

## Ringkasan

Melanjutkan paket **Full Engine** dengan beberapa fondasi operasional sekaligus: import retail lebih matang, filter riwayat transaksi, workflow expired, onboarding checklist, preset pekerjaan karyawan, dan input cepat SKU di barang masuk.

## Implementasi

- `src/app/(app)/inventory/import/page.tsx`
  - Parser CSV diperbaiki agar mendukung quoted comma.
  - Template CSV diperluas dengan kolom `Outlet`, `Supplier`, `TrackExpiry`, `Batch`, dan `ExpirationDate`.
  - Preview import menampilkan outlet dan expired date.

- `src/app/(app)/inventory/actions.ts`
  - `importProductsBulkAction()` sekarang validasi duplikat SKU/barcode di CSV dan database.
  - Import dapat memilih outlet berdasarkan nama cabang.
  - Import dapat membuat supplier jika kolom supplier baru.
  - Import dapat membuat `StockBatch` untuk stok awal jika ada batch/expired.
  - `trackExpiry` aktif otomatis jika ada tanggal expired.
  - Menambahkan `disposeExpiredBatchAction()` untuk buang/rusak batch expired.

- `src/components/inventory/expired-batch-actions.tsx`
  - Komponen aksi buang/rusak batch expired dengan catatan.

- `src/app/(app)/inventory/page.tsx`
  - Panel batch mendekati expired sekarang punya aksi buang/rusak.

- `src/app/(app)/kasir/riwayat/page.tsx`
  - Filter transaksi berdasarkan metode bayar dan status.

- `src/app/(app)/onboarding/page.tsx`
  - Halaman checklist setup toko: profil bisnis, outlet, produk, staff, barang masuk, dan tes transaksi.

- `src/app/(app)/simple/menu/page.tsx`
  - Menambahkan shortcut **Setup Toko** ke onboarding.

- `src/components/pengaturan/user-form-modal.tsx`
  - Preset pekerjaan: Kasir, Inventory, Finance, HR, Manager Cabang, Dapur, Laundry, Runner.

- `src/components/stock-receipt/stock-receipt-manager.tsx`
  - Form Barang Masuk punya input cepat scan/ketik SKU/nama produk.

## Verifikasi

- `npx tsc --noEmit`: sukses.

## Catatan

- Permission granular belum menjadi enum auth baru. Saat ini preset pekerjaan disimpan di `jobTitle` sebagai langkah aman sebelum refactor role besar.
- Kamera barcode dan foto nota masih pending.
